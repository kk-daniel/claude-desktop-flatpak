app-id: ai.claude.Claude
runtime: org.freedesktop.Sdk
runtime-version: "25.08"
sdk: org.freedesktop.Sdk
base: org.electronjs.Electron2.BaseApp
base-version: "25.08"
command: claude-desktop
tags: [proprietary]
separate-locales: false
add-extensions:
  com.visualstudio.code.tool:
    directory: tools
    subdirectories: true
    version: '25.08'
    add-ld-path: lib
    no-autodownload: true
    autodelete: false
  org.freedesktop.Sdk.Extension.node24:
    directory: lib/sdk/node24
    version: '25.08'
    add-ld-path: lib
    no-autodownload: false
    autodelete: false
finish-args:
  - --require-version=0.10.3
  - --share=network
  - --share=ipc
  - --socket=wayland
  - --socket=fallback-x11
  - --device=dri
  - --env=LD_LIBRARY_PATH=/app/lib
  - --env=XCURSOR_PATH=/run/host/user-share/icons:/run/host/share/icons
  - --env=ELECTRON_OZONE_PLATFORM_HINT=wayland
  # Sandbox-private ~/.claude for plugin manifests, marketplace cache, and
  # other Claude Code-shared state. Mapped at $HOME/.claude inside the
  # sandbox, stored at ~/.var/app/ai.claude.Claude/.claude/ on the host —
  # not shared with a host Claude Code CLI install.
  - --persist=.claude
  - --talk-name=org.freedesktop.Notifications
  # Keyring backend for Chromium's os_crypt, which picks the backend from
  # XDG_CURRENT_DESKTOP. Without a reachable one, encryption falls back to the
  # plaintext "basic" store and the app then refuses to persist a login at all
  # ("Your sign-in won't be saved on this device"). Both names are D-Bus
  # activatable, so kwalletd starts on demand without org.kde.klauncher5.
  # The generic org.freedesktop.secrets is deliberately not granted: Secret
  # Service has no per-application access control, so the grant would also
  # expose every other unlocked secret on the session bus. That costs login
  # persistence on GNOME-style desktops, where libsecret is the only backend
  # Chromium will pick. Electron's safeStorage is built on the synchronous
  # OSCrypt, so xdg-desktop-portal's per-app Secret API cannot substitute --
  # its key provider lives in os_crypt::async and never reaches safeStorage.
  - --talk-name=org.kde.kwalletd5
  - --talk-name=org.kde.kwalletd6
  # Notification-area / tray (StatusNotifierItem on KDE & GNOME-via-extension,
  # AppIndicator on Ubuntu/Cinnamon). Without these the SNI registration
  # silently fails under Flatpak and no icon appears. Modern Electron
  # registers the item by object path on its existing bus connection, so
  # we don't need to --own-name the per-PID SNI bus name.
  - --talk-name=org.kde.StatusNotifierWatcher
  - --talk-name=com.canonical.AppMenu.Registrar
  - --talk-name=com.canonical.AppMenu.Registrar.*
  - --talk-name=com.canonical.indicator.application
  # libappindicator claims a per-PID bus name under
  # com.canonical.indicator.application; without ownership, the
  # registration call to the watcher silently no-ops.
  - --own-name=com.canonical.indicator.application.*
cleanup:
  - /include
  - /lib/pkgconfig
  - /share/gtk-doc
  - '*.la'
modules:
  # Needed only to crack the .deb's `ar` container at install time: binutils
  # lives in the SDK, but apply_extra runs against the Platform runtime. 7z
  # unwraps the ar member and its xz stream in one pass, yielding data.tar.
  - name: 7zip
    buildsystem: simple
    build-commands:
      - install -Dm755 7zz /app/bin/7zz
      - ln -s 7zz /app/bin/7z
    sources:
      - type: archive
        only-arches: [x86_64]
        url: https://github.com/ip7z/7zip/releases/download/26.03/7z2603-linux-x64.tar.xz
        sha256: dc99eff5008f1ab79bd7084c68513701547a808a89502bf4133683535ab3c695
        strip-components: 0
      - type: archive
        only-arches: [aarch64]
        url: https://github.com/ip7z/7zip/releases/download/26.03/7z2603-linux-arm64.tar.xz
        sha256: 2389ba20e4d8295e8709c20b6263b69bd1ec4972fe38a04ad7a1badbf595b996
        strip-components: 0

  - name: claude
    buildsystem: simple
    build-commands:
      - install -Dm755 claude.sh /app/bin/claude-desktop
      - install -Dm755 apply_extra /app/bin/apply_extra
      - install -Dm644 ${FLATPAK_ID}.metainfo.xml -t /app/share/metainfo
      - install -Dm644 ${FLATPAK_ID}.desktop -t /app/share/applications
      - install -Dm644 ${FLATPAK_ID}-url-handler.desktop -t /app/share/applications
      - install -Dm644 ${FLATPAK_ID}.svg /app/share/icons/hicolor/scalable/apps/${FLATPAK_ID}.svg
      - mkdir -p /app/extra/claude
      - mkdir -p /app/lib/sdk/node24
      - mkdir -p /app/tools
      - ln -s /app/lib/sdk/node24/bin/node /app/bin/node
      - ln -s /app/lib/sdk/node24/bin/npm /app/bin/npm
      - ln -s /app/lib/sdk/node24/bin/npx /app/bin/npx
      - ln -s /app/lib/sdk/node24/bin/corepack /app/bin/corepack
    sources:
      # Anthropic's official Linux build, from their APT repo. The .deb carries
      # its own Electron (see usr/lib/claude-desktop/version), the real
      # @ant/claude-native and node-pty binaries, and the Linux tray/locale
      # resources — so no Electron download, asar repack, or native stubs are
      # needed. Index:
      # https://downloads.claude.ai/claude-desktop/apt/stable/dists/stable/main/binary-amd64/Packages
      - type: extra-data
        filename: claude-desktop-amd64.deb
        only-arches: [x86_64]
        url: https://downloads.claude.ai/claude-desktop/apt/stable/pool/main/c/claude-desktop/claude-desktop_1.46388.2_amd64.deb
        sha256: 98bf54e85e4916068c4281459b0f0431d8ff68034773f3ee98311d7206566ab1
        size: 168494808
      - type: extra-data
        filename: claude-desktop-arm64.deb
        only-arches: [aarch64]
        url: https://downloads.claude.ai/claude-desktop/apt/stable/pool/main/c/claude-desktop/claude-desktop_1.46388.2_arm64.deb
        sha256: b944a2154528815bb476bfb1e7091f8a3e329b3c67f6c11de8d41668a603bd9e
        size: 161542836
      - type: file
        path: apply_extra
      - type: file
        path: claude.sh
      - type: file
        path: ai.claude.Claude.metainfo.xml
      - type: file
        path: ai.claude.Claude.desktop
      - type: file
        path: ai.claude.Claude-url-handler.desktop
      - type: file
        path: ai.claude.Claude.svg
